# Build output — regenerated by `anycms build`, do not commit.
/public/

# Local anycms state: plugin install records (.anycms/plugins.json) and the
# deploy working repos (.anycms/deploy/<target>/, each with its own .git).
/.anycms/
